<%= form_for(job) do |f| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company %>
    <%= f.text_field :company %>
  </div>

  <div class="field">
    <%= f.label :URL %>
    <%= f.text_field :URL %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :date_applied %>
    <%= f.date_field :date_applied %>
  </div>

  <div class="field">
    <%= f.label :resume_sent %>
    <%= f.check_box :resume_sent %>
  </div>

  <div class="field">
    <%= f.label :cover_letter_sent %>
    <%= f.check_box :cover_letter_sent %>
  </div>

  <div class="field">
    <%= f.label :cover_letter %>
    <%= f.text_area :cover_letter, cols: 100, rows:20 %>
  </div>

  <div class="field">
    <%= f.label :not_offered %>
    <%= f.date_field :not_offered %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
